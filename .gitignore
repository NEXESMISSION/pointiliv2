# dependencies
/node_modules

# next.js
/.next/
/.next-*/
/out/
/build

# misc
.DS_Store
*.pem
*.tsbuildinfo
next-env.d.ts

# debug
npm-debug.log*

# env files: .env.local holds live Supabase secrets and must never be committed
.env*
!.env.example

# vercel
.vercel

# local scratch / test output
scratch/
.e2e/
/supabase/.temp/

# rendered carousels (re-made with social/carousels/render.mjs) and scratch reviews
social/carousels/out/
social/carousels/review/

# the CRM holds real customers' names, numbers and DMs — it never goes in a repo
/social/business/crm/
